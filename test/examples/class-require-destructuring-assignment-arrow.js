define(function (require) {
    const { test } = require('some-package');
    const home = require('some').home;
    const _ = require('loadsh');
    const $ = require('jquery');

    class A {
    }

    return A;
});
